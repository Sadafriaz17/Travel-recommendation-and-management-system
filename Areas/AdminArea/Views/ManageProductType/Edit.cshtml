@model TourwebsiteFYP.DB_data_models.ProductType
@{
    ViewBag.Title = "Edit Product Type";
    ViewBag.ActiveMenu = "Products";
    ViewBag.ActivePage = "ProductTypes";
}
<div class="page-header">
    <div><div class="page-title">Edit Product Type</div><div class="page-subtitle">Update type #@Model.ProductTypeId</div></div>
    <a href="@Url.Action("Index","ManageProductType", new { area = "AdminArea" })" class="btn-admin btn-admin-outline"><i class="bi bi-arrow-left"></i> Back</a>
</div>
<div class="admin-card">
    <div class="admin-card-header"><div class="admin-card-title"><i class="bi bi-pencil-square"></i> Product Type Details</div></div>
    <div class="admin-card-body">
        @using (Html.BeginForm("Edit","ManageProductType", new { area = "AdminArea" }, FormMethod.Post))
        {
            <input type="hidden" name="ProductTypeId" value="@Model.ProductTypeId" />
            <div class="form-grid">
                <div class="form-group form-grid-1">
                    <label class="form-label">Type Name <span>*</span></label>
                    <input type="text" name="TypeName" value="@Model.TypeName" class="form-control" required />
                </div>
            </div>
            <div class="form-actions">
                <button type="submit" class="btn-admin btn-admin-primary"><i class="bi bi-floppy"></i> Update Type</button>
                <a href="@Url.Action("Index","ManageProductType", new { area = "AdminArea" })" class="btn-admin btn-admin-outline">Cancel</a>
            </div>
        }
    </div>
</div>