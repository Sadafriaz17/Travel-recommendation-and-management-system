@model IEnumerable<TourwebsiteFYP.DB_data_models.ProductType>
@{
    ViewBag.Title = "Manage Product Types";
    ViewBag.ActiveMenu = "Products";
    ViewBag.ActivePage = "ProductTypes";
}
<div class="page-header">
    <div><div class="page-title">Product Types</div><div class="page-subtitle">Manage categories for travel products</div></div>
    <a href="@Url.Action("Create","ManageProductType", new { area = "AdminArea" })" class="btn-admin btn-admin-primary"><i class="bi bi-plus-circle"></i> Add Type</a>
</div>
@if (TempData["SuccessMessage"] != null)
{ <div class="alert-toast alert-toast-success"><i class="bi bi-check-circle-fill"></i> @TempData["SuccessMessage"] <button class="alert-close"><i class="bi bi-x"></i></button></div> }
@if (TempData["ErrorMessage"] != null)
{ <div class="alert-toast alert-toast-error"><i class="bi bi-exclamation-triangle-fill"></i> @TempData["ErrorMessage"] <button class="alert-close"><i class="bi bi-x"></i></button></div> }
<div class="admin-card">
    <div class="admin-card-header">
        <div class="admin-card-title"><i class="bi bi-tags"></i> All Product Types</div>
        <span style="font-size:12px;color:var(--text-muted)">@Model.Count() records</span>
    </div>
    <div class="admin-card-body-flush">
        @if (Model.Any())
        {
            <div class="admin-table-wrap">
                <table class="admin-table">
                    <thead><tr><th>#ID</th><th>Type Name</th><th>Actions</th></tr></thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td style="font-size:12px;color:var(--text-muted);font-weight:600">#@item.ProductTypeId</td>
                                <td style="font-weight:600">@item.TypeName</td>
                                <td>
                                    <div class="action-group">
                                        <a href="@Url.Action("Edit","ManageProductType", new { area="AdminArea", id=item.ProductTypeId })" class="btn-icon btn-icon-edit"><i class="bi bi-pencil"></i></a>
                                        <a href="@Url.Action("Delete","ManageProductType", new { area="AdminArea", id=item.ProductTypeId })" class="btn-icon btn-icon-delete" onclick="return confirm('Delete this product type?')"><i class="bi bi-trash"></i></a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="empty-state">
                <div class="empty-state-icon"><i class="bi bi-tags"></i></div>
                <h4>No Product Types</h4>
                <p>Add product categories (e.g. Gear, Accessories).</p>
                <a href="@Url.Action("Create","ManageProductType", new { area = "AdminArea" })" class="btn-admin btn-admin-primary"><i class="bi bi-plus-circle"></i> Add Type</a>
            </div>
        }
    </div>
</div>