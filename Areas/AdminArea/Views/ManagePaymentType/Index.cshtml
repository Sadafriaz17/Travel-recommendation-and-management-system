@model IEnumerable<TourwebsiteFYP.DB_data_models.PaymentType>
@{
    ViewBag.Title = "Manage Payment Types";
    ViewBag.ActiveMenu = "Payments";
    ViewBag.ActivePage = "PaymentTypes";
}
<div class="page-header">
    <div><div class="page-title">Payment Types</div><div class="page-subtitle">Manage payment methods</div></div>
    <a href="@Url.Action("Create","ManagePaymentType", new { area = "AdminArea" })" class="btn-admin btn-admin-primary"><i class="bi bi-plus-circle"></i> Add Type</a>
</div>
@if (TempData["SuccessMessage"] != null)
{ <div class="alert-toast alert-toast-success"><i class="bi bi-check-circle-fill"></i> @TempData["SuccessMessage"] <button class="alert-close"><i class="bi bi-x"></i></button></div> }
@if (TempData["ErrorMessage"] != null)
{ <div class="alert-toast alert-toast-error"><i class="bi bi-exclamation-triangle-fill"></i> @TempData["ErrorMessage"] <button class="alert-close"><i class="bi bi-x"></i></button></div> }
<div class="admin-card">
    <div class="admin-card-header">
        <div class="admin-card-title"><i class="bi bi-credit-card-2-front"></i> All Payment Types</div>
        <span style="font-size:12px;color:var(--text-muted)">@Model.Count() records</span>
    </div>
    <div class="admin-card-body-flush">
        @if (Model.Any())
        {
            <div class="admin-table-wrap">
                <table class="admin-table">
                    <thead><tr><th>#ID</th><th>Type Name</th><th>Actions</th></tr></thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td style="font-size:12px;color:var(--text-muted);font-weight:600">#@item.PaymentTypeId</td>
                                <td style="font-weight:600">@item.PaymentTypeName</td>
                                <td>
                                    <div class="action-group">
                                        <a href="@Url.Action("Edit","ManagePaymentType", new { area="AdminArea", id=item.PaymentTypeId })" class="btn-icon btn-icon-edit"><i class="bi bi-pencil"></i></a>
                                        <a href="@Url.Action("Delete","ManagePaymentType", new { area="AdminArea", id=item.PaymentTypeId })" class="btn-icon btn-icon-delete" onclick="return confirm('Delete this payment type?')"><i class="bi bi-trash"></i></a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="empty-state">
                <div class="empty-state-icon"><i class="bi bi-credit-card-2-front"></i></div>
                <h4>No Payment Types</h4>
                <p>Add payment methods (e.g. Visa, PayPal).</p>
                <a href="@Url.Action("Create","ManagePaymentType", new { area = "AdminArea" })" class="btn-admin btn-admin-primary"><i class="bi bi-plus-circle"></i> Add Type</a>
            </div>
        }
    </div>
</div>
